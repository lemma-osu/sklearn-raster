# Installation

From PyPI:

```bash
pip install sklearn-raster
```

Optional extras can be installed with:

```bash
pip install sklearn-raster[tutorials]
```

## Dependencies

- Python >= 3.9
- scikit-learn >= 1.2
- numpy
- dask
- typing-extensions